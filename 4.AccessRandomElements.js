let acessRandomElements=(arr)=>{
    let result=[first, second, last]=[arr[0], arr[1], arr[arr.length-1]];
    return result;
}
let arr=[1,4,2,5,6,3,8];
let result=acessRandomElements(arr);
console.log(result);